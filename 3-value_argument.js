// Check if there are any arguments after node and script path
if (process.argv[2]) {
    console.log(process.argv[2]);  // Print the first argument
} else {
    console.log("No argument");    // No arguments found
}
